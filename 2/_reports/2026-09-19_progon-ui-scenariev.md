# Отчёт: прогон UI-сценариев по прототипу — 19.09.2026

**Что делали:** прошли вживую по клик-путям из `xHOSM_сценарии_интерфейса_v2.md` (36 сценариев: UI-00…UI-D-02) на прототипе `2/index.html`.
**Как проверяли:** реальные клики и ввод в браузере (`http://127.0.0.1:8123/XHosm/2/index.html`): для каждого шага фиксировали результат (открылся экран/диалог, что показал снекбар), сверяли с блоком «> Новому UI» у сценария.
**Итог:** 36/36 путей пройдены; **33 закрыты полностью ✅**, 3 обозначены ◐ (см. ниже). В ходе прогона найдено и починено 6 дефектов; все правки — в JS-слое файла, разметка и ДС не менялись; 0 ошибок JS.

---

## ① Общее (UI-00…UI-03)

- **UI-00 Вход:** аутентификация — вне объёма прототипа; след SSO — в рельсе («Анна К. · L3 Support · SSO, 8 ч»). Панель переосмыслена: рельс (Работа / Объекты / Инструменты) + топбар: крошки, омнибокс, «Очередь: 8» (клик → Центр задач), уведомления («Уведомлений нет»), «?» («Открылась вики xHOSM»). Пустая таблица «до первого поиска» заменена «Рабочим столом смены» (DO-899). История операций по клику на имя — не смоделирована (формат ждёт решения).
- **UI-01 Поиск — 6/6:** «Найти» → «Поиск выполнен: найдено 137 серверов (показаны первые 25) · [К результатам]»; Shift+«Связанные» → «Chain 6711911 ↔ RMS (2 точки)»; «Экспорт CSV» → «137 строк»; фильтры-чипсы ДО таблицы + сохранённые выборки; WHERE — под раскрытием (только чтение, таймаут 30 с).
- **UI-02 Строка результата — переосмыслено:** флажок = выбор для массовых (слежение — постоянное, в статус-карточке: источник, свежесть, история, автообновление Off/20/60/300/900); «Больше информации» / «Внутренний порт» / «Ревизия» — есть; UID · iikoUser · Контракт — reveal (проверено: «••••••••» → «iiko#c7x2»); «История» — вкладки карточек клиента и сервера; ссылки строки ведут в карточки/CRM/логи.
- **UI-03 Планировщик:** «Очередь: 8» из шапки — из любой точки; фильтры «Все статусы/Все типы» + чипсы «Только мои/тех.окно»; «Исполнить сейчас» → «Задача поднята в начало очереди…»; отмена → «Задача отменена до выполнения (BR-SCH-005)»; «Снять дубли» → «Найдены и сняты 2 налипшие задачи (DO-2004)».

## ② L3-ядро (UI-L3-01…17) — 15 живых кликов

Полностью ✅: 01 (Остановка → «статус → Stopped/Manual · К задачам»; Перезапуск → план-диалог «Остановка→Запуск→Проверка»), 02 (отвязка UID кнопкой и Ctrl+Enter; маршрут UOC 3 шага + фолбэк Bcrypt/resetPasswordHashes), 03 (db-period-length-days + правила у поля), 04 (memory + «×1.5–2, не уменьшать», BR-MEM), 05 (флаги Downgrade/Unlock/Rapid с пояснением рисков; блок «что будет во время обновления»), 06 (live tail -F, поиск, диапазон дат, ZIP, «поделиться»; Victoria — ссылка в мониторингах), 07 (FTP + анонимизация; «Бэкап запущен… · К задачам»), 08 (восстановление: «период станет 60 дней»; перенос: push-таблицы), 09 (смены: «открытых нет» → «К списку смен»; массово — «до даты» волнами), 10 (SQR/SQW/Groovy, копилка + история), 11 (мастер shrink, 6 шагов — эталон), 14 (восстановление из архива, «до 15 минут»), 15 (= маршрут UOC), 17 (FTP-выдача).
Обозначено ◐: 12 (RMS→Chain — мастер в списке, шаги не развёрнуты), 13 (демо-стенд — «Демо-сервер» + мастера; ЛК partners вне), 16 (франшиза — RESTOPROPPACK/мастер обозначены).
Нет в прототипе: «фикс статуса CONTAINERCREATING»; DO-1311 («мои демо»), DO-1917/1181 («срок доступа»).

## ③ L3 Web (UI-W-01/02)

- .xhosm из строки: «Файл .xhosm скачан — открыть через xHosmLauncher.bat» (для чейна — ChainOffice).
- Конфигурация → «Сохранить»: «Настройки сохранены; служба перезапускается. Прогресс — в Центре задач».
- Вне покрытия: вход в веб клиента (поле «W»), параметры uoc-server-url / developer-crmid-iikotransport.

## ④ Аналитики (UI-A-01…04)

Шаблон «Собрать статистику»; «Сохранить шаблон» → «Сохранено как шаблон…»; «Экспорт отчёта» → «137 строк с результатами»; «Повторить ошибочные» → «только по 3 ошибочным»; BTV + Branch + мастер; SQL-копилка + история запусков.

## ⑤ Инфраструктура (UI-I-01…04)

NSR-подсказка с правилами домена; переезд = «Перенос/копирование»; архивы: Архивация / Восстановление / Переименование (без «Удаления» — уточнить); массовые из мониторинга: «вставка до 1000 — UI-I-04», «Показаны только выбранные: 137 строк».

## ⑥ Партнёры (UI-P-01…03)

Партнёрский режим живой: «Режим: Партнёр (внешний). Доступны просмотр и iikoUser; операции скрыты — „почему“ в подсказках у недоступных элементов»; FTP-выдача. Нюансы: «Создание папки на FTP» отдельной кнопкой нет; англ. локализация (DO-1218) не моделируется.

## ⑦ Разработка (UI-D-01/02)

D-01 закрыт (поиск → логи/Victoria/ZIP → конфигурация на просмотр). D-02 — вне объёма (сборки уходят из xHOSM в отдельный контур).

---

## Найденные дефекты → починено в ходе прогона

| № | Дефект | Фикс |
|---|---|---|
| 1 | Ctrl+Enter не работал в главном поле поиска (подсказка обещала) | keydown: `#v-search` → тот же снекбар, что у «Найти» |
| 2 | Флажки без подписи не кликались мышью (нативный инпут скрыт в ДС) | делегирование клика по `.ds-checkbox__box/.ds-radio__box` вне label |
| 3 | «Выбрать всё» в шапке зажигалось от одной строки (латентный баг updateSel) | считаем по таблице (`[data-check-scope]` → `[data-check-all]`) |
| 4 | Переключатели времени и шаги степперов — статичны | делегирование `.ds-button-toggle .ds-btn` и `.ds-step` |
| 5 | Чипсы-шаблоны/фильтры — статичны | крестик снимает чипс; клик выбирает один в группе |
| 6 | Не было промежуточного состояния «выбрать всё» | `indeterminate` из ДС при частичном выборе |

## Открытые вопросы (ждут решений)

- «История» по клику на имя пользователя (формат); действия строк по ховеру (показывать ли всегда); BR-коды под роли; селекты-«витрины».
- Хвосты покрытия: CONTAINERCREATING-фикс; DO-1311 / DO-1917 / DO-1181; поле «W»; uoc-параметры; «Удаление» сервера; «Создание папки на FTP».
